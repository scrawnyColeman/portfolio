import "./style.scss";
import Widget from "../UI/Widget";

const Profile = () => {
  return (
    <div className="profile-container widget">
      <Widget title="Personal Profile">Body</Widget>
    </div>
  );
};

export default Profile;
